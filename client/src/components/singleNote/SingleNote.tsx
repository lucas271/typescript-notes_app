import { Note } from "../../models/note"

interface Props{
    note: Note,
    notes: Note[] 
}

const SingleNote: React.FC<Props> = ({notes, note}) => {
    return <>
    </> 
}

export default SingleNote